### Idle configuration
#
# Example configuration:
#
# exec swayidle -w \
#          timeout 300 'swaylock -f -c 000000' \
#          timeout 600 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
#          before-sleep 'swaylock -f -c 000000'
#
# This will lock your screen after 300 seconds of inactivity, then turn off
# your displays after another 300 seconds, and turn your screens back on when
# resumed. It will also lock your screen before your computer goes to sleep.

### Idle configuration
#
# Example configuration:
#
# screen privacy:
# make use of systemd logind idlehint feature, to prevent
# whatever on the screen immediately before suspend is shown

# details: --locked says the bindswitch command also works in lock screen
#          swayidle idlehint 2 let logind execute sleep command after
#          two seconds
bindswitch --locked lid:toggle exec swaylock --daemonize && systemctl sleep

# lock screen after 300 seconds of inactivity
# then let logind execute sleep command
# will not work without quotes
# if screen lock is on for 60s; sleep again
 exec swayidle -w \
          timeout 60  'if pgrep swaylock; then systemctl sleep; fi' \
          timeout 150 'swaylock --daemonize' \
          timeout 155 'systemctl sleep'

#
# This will lock your screen after 300 seconds of inactivity, then turn off
# your displays after another 300 seconds, and turn your screens back on when
# resumed. It will also lock your screen before your computer goes to sleep.
